version: 2

models:
  - name: int_user_order_agg
    columns:
      - name: user_id 
        description: This is an unique identifier for the product
        test: 
          - not_null
          - unique
      - name: order_length_hour
        test:
          - non_negative

 
